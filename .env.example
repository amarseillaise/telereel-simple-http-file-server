# Port the HTTP server listens on
SERVER_PORT=8080

# Directory for storing downloaded video content
CONTENT_DIR=./content

# Optional: paths to TLS certificate and key for direct HTTPS (without reverse proxy)
TLS_CERT_FILE=
TLS_KEY_FILE=
